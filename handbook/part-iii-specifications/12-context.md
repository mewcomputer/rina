# Chapter 12 — Context Specification

A context references existing objects and never duplicates or owns them. Removing an object from a context must not delete it. Resolution should be deterministic for equal inputs and should record material omissions.
