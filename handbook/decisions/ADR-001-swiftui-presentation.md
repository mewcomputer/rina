# ADR — SwiftUI as primary presentation

**Status:** Accepted

## Decision
Use SwiftUI first; introduce UIKit only through narrow interoperability boundaries.

## Consequences
The choice creates a stable architectural direction while preserving replaceable implementation boundaries.
