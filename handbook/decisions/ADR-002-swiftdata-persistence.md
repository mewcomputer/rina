# ADR — SwiftData for structured persistence

**Status:** Accepted

## Decision
Use SwiftData behind repository protocols for structured records.

## Consequences
The choice creates a stable architectural direction while preserving replaceable implementation boundaries.
